version: 2

models:
  - name: dim_channels
    description: "Dimension table for Telegram channels"
    columns:
      - name: channel_name
        description: "Name of the Telegram channel"
        tests:
          - not_null
          - unique
